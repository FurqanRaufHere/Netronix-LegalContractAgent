# Example environment variables for Netronix-LegalContractAgent
# Copy this file to `.env` and fill with real secrets for local development.

# GROQ (LLM) settings
# Obtain a GROQ API key and set it here
GROQ_API_KEY=YOUR_GROQ_API_KEY_HERE
# Optional: custom base URL if using a proxy or different endpoint
GROQ_BASE_URL=https://api.groq.com/openai/v1
# Model name used for analysis (override if needed)
GROQ_MODEL=llama-3.3-70b-versatile

# Retry/backoff settings (helpful in deployed environments)
# Number of attempts (integer)
GROQ_MAX_RETRIES=3
# Base retry delay in seconds (exponential backoff multiplies this)
GROQ_RETRY_BASE_DELAY=1.0
# Optional throttle between requests (milliseconds). Set >0 to reduce burst rate.
GROQ_THROTTLE_DELAY_MS=0
# When true, include additional LLM error details in logs/traces (DEV ONLY)
DEBUG_LLM_ERRORS=false

# Resend email settings
RESEND_API_KEY=YOUR_RESEND_API_KEY_HERE
EMAIL_BACKEND=resend  # or "smtp" to force SMTP
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_OWNER_EMAIL=you@example.com

# SMTP fallback (for Mailtrap, Gmail app password, etc.)
# Use these when EMAIL_BACKEND=smtp or when Resend rejects the from-address
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password
SMTP_FROM_EMAIL=you@example.com

# LangChain / tracing (optional)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=YOUR_LANGCHAIN_API_KEY
LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
LANGCHAIN_PROJECT=default

# App behavior toggles
# Set to "true" in local dev to surface more debug info (do not enable publicly)
SHOW_DEBUG_UI=false

# Logging / misc
LOG_DIR=logs
